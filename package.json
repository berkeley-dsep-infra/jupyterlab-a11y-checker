{
  "name": "a11y-checker-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "jlpm workspaces foreach -At run build",
    "lint": "jlpm workspaces foreach -A run lint"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.49.0",
    "husky": "^9.0.11",
    "prettier": "^3.7.3",
    "typescript": "~5.4.0"
  }
}
