{
  "name": "a11y-checker-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/core && jlpm build && cd ../cli && jlpm build && cd ../extension && jlpm build && jupyter labextension develop --overwrite .",
    "lint": "cd packages/extension && jlpm lint"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.49.0",
    "husky": "^9.0.11",
    "prettier": "^3.7.3",
    "typescript": "~5.4.0"
  }
}
